---
- name: Docker | EE | Debian | Ensure all APT prerequisites are met on Jessie/Stretch
  apt:
    name: "{{ item }}"
    state: present
  with_items:
    - apt-transport-https
    - ca-certificates
    - curl
    - gnupg2
    - software-properties-common
